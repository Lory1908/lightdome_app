class AppFeatures {
  // Toggle external music providers. Keep false until service choice is made.
  static const bool enableSpotify = false;
  static const bool enableOpenProvider = false; // local file → .ldy
}

